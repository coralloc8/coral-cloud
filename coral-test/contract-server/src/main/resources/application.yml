info:
  author: huss
  date: 2021年6月7日
  remark: 契约
  my-server-name: contract-server

server:
  port: 8087

#spring
spring:
  application:
    name: ${info.my-server-name}
  profiles:
    active: dev
  devtools:
    restart:
      enabled: true
#  main:
#    allow-bean-definition-overriding: true